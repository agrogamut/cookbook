-- Deliberately not restoring 'claude-authored' -- that value should never exist in this
-- database again. Down here means "leave the corrected label in place", not "reintroduce
-- the rule breach".
SELECT 1;
